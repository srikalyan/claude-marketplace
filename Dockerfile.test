FROM python:3.11-slim

RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY .claude-plugin/ .claude-plugin/
COPY tests/ tests/

CMD ["python3", "tests/test_marketplace.py"]
